import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    grid: {
        flexGrow: 1,
    },
    gridTypo: {
        // fontSize: '35px',
        marginLeft: '5%',
        marginTop: '25px',
        marginBottom: '20px'
    }
  }));